#!/bin/bash
# Legacy helper kept only as documentation.
# opus-mt now downloads runtime + model on demand from Diagnostics and is no longer bundled.

set -e

echo "opus-mt local bundling is disabled."
echo "Use the Diagnostics screen to download runtime + model on demand."
